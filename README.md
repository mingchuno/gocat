# gocat

A POSIX compliance cat in Go

## Usage

```
gocat is cat

Usage:
  cat [flags]

Flags:
  -h, --help       help for cat
  -u, --nobuffer   Disable output buffering.
```

## Build

```bash
make build
```
